//Use JavaScript to add alot of className to the Tags

var links = document.getElementsByTagName("TagName");  // TagName can be "a", "p"...

for(i=0; i<links.length; i++){
links[i].className = "link-" + i;
}


// NaN: Not a Number
var a= "xyz";
var b= 5;
console.log(a+b);   // Output: NaN

/*DOM: Document Object Model
Document are web Pages, Elements are Objects , Model is in tree structure (add, remove, change);y0000000000000
*/
document.getElementsByTagName(" ");
document.getElementsByClassName(" ");
document.getElementById(" ");   // ID is only 1 thats why Element not Elements

e.g.
var link = document.getElementById("test");
link.getAttribute("href");

//Changing CSS Style

link.setAttribute("style", "left: 10px;"):

var newA = document.createElement("a");  

menu.appendChild(newA);  //also removeChild
newB.innerHTML = "blog";
menu.insertBefore(newA, menu.getElementsByTagName("li")[0]);   // block the menu's tag 


//EVENTS
alert(" ");
newA.onclick = function(){};
newA.onmouseover = function(){};

window.onLoad = function(){}; //when all the HTML tags have loaded in then fire the js functions for the proper flow of the webpage.


Jquery: Its a JavaScript library. It makes things like HTML document traversal and manipulation, event handling, animation, and Ajax much simpler with an easy-to-use API that works across a multitude of browsers.

AJAX: allows to communicate with the server without having to leave the webpage. (dynamically load the data)

JSON: Its a way of organizing data and works well with AJAX.
